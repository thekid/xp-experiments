package test;
import peer.Socket;

public interface Protocol {

  public void accept(Socket $s) default DefaultAcceptHandler::accept;
  
  public void connect(Socket $s);
  
  public void data(Socket $s);

  public void close(Socket $s);
}
