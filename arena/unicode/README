Unicode
========================================================================

Created merged XP (local changes in src/, rest in checkout / release):
<pre>
  $ export USE_XP='src;~/devel/xp/trunk/'
</pre>

