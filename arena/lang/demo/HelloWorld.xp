import util.cmd.Console;
import util.*;

public class HelloWorld {

  public static void main(string[] $args) {
    $now= new Date()->toString('Y-m-d');
    if ($args[0]) {
      Console::writeLine('Hello ', $args[0], ' @ ', $now);
    } else {
      Console::writeLine('Hello World @ ', $now);
    }
  }
}
