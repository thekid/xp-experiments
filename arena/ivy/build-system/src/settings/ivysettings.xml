<?xml version="1.0"?>
<ivysettings>
  <settings defaultResolver="shared"/>

  <!-- settings for public XP repository -->
  <include url="ivysettings-public.xml"/>

  <!-- repository settings -->
  <include url="${ivy.home}/ivysettings-repositories.xml"/>

  <!-- credentials file -->
  <include url="${ivy.home}/ivysettings-credentials.xml"/>

  <!-- global "ivy" local repository configuration -->
  <include url="${ivy.default.settings.dir}/ivysettings-local.xml"/>
</ivysettings>
