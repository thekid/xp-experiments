<?xml version="1.0"?>
<project name="go-ivy">
  <!-- Automatically download ivy & load library -->
  <property name="ivy.install.version" value="2.2.0" />
  <property name="ivy.jar.dir" value="${user.home}/.ant/lib" />
  <property name="ivy.jar.file" value="${ivy.jar.dir}/ivy-${ivy.install.version}.jar" />

  <available property="ivy.detected" file="${ivy.jar.file}"/>
  <target name="download-ivy" unless="ivy.detected" description="--> Download ivy">
  	<mkdir dir="${ivy.jar.dir}"/>

    <echo message="Downloading &amp; loading ivy..."/>
  	<get
     src="http://repo1.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar"
     dest="${ivy.jar.file}" 
     usetimestamp="true"
    />
  </target>
  
  <target name="install-ivy" depends="download-ivy" unless="ivy.detected" description="--> Load ivy jar">
  	<path id="ivy.lib.path">
  	  <fileset dir="${ivy.jar.dir}" includes="*.jar"/>
  	</path>
  	<taskdef 
     resource="org/apache/ivy/ant/antlib.xml"
  	 uri="antlib:org.apache.ivy.ant"
     classpathref="ivy.lib.path"
    />
  </target>  
</project>
