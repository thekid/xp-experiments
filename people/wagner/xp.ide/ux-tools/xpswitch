#!/bin/sh

SYMLINK=~/.xp/xp.ini
SOURCE=~/.xp/xp-${1:-"current"}.ini

if [ -f ${SOURCE} ]
then
  if [ -h ${SYMLINK} ]
  then
    rm ${SYMLINK}
  fi
  ln -s ${SOURCE} ${SYMLINK}
  ls -l ${SYMLINK}
else
  echo "Error: "${TARGET}" does not exist" >&2
  exit 1
fi

